const { spawnSync } = require('child_process'); const ffmpeg = require('ffmpeg-static'); const path = require('path'); const videoPath = path.join(process.cwd(), 'public', 'Man_looking_at_camera_202606221900.mp4'); const outPath = path.join(process.cwd(), 'public', 'frames1', 'frame%04d.png'); console.log('Extracting frames using ffmpeg:', ffmpeg); const result = spawnSync(ffmpeg, ['-i', videoPath, '-q:v', '2', outPath]); console.log('Done ffmpeg', result.status);
